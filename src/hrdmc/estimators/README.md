# Estimators

Owner: observables computed directly from sampled coordinates.

This package owns coordinate estimators such as density profiles, pair distributions, and structure factors. It should not own Monte Carlo sampling, theory approximations, or benchmark comparison logic.
